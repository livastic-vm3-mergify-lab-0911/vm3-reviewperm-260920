name: VM3 dual check success 260925
on:
  pull_request:
    branches:
      - main
jobs:
  gate:
    name: vm3-dual-event-gate
    runs-on: ubuntu-latest
    steps:
      - name: Early success
        run: echo "earlier check intentionally succeeds"
